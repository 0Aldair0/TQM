<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Para Camila 💖</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body, html {
      height: 100%;
      background: black;
      overflow: hidden;
      font-family: 'Segoe UI', sans-serif;
    }
    canvas {
      position: absolute;
      top: 0;
      left: 0;
    }
    .centered {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      color: white;
      text-align: center;
      z-index: 2;
    }
    .hidden {
      display: none;
    }
    .password-box {
      margin-top: 20px;
    }
    input[type="password"] {
      padding: 10px;
      font-size: 18px;
      border-radius: 10px;
      border: none;
      outline: none;
    }
    button {
      margin-top: 10px;
      padding: 10px 20px;
      font-size: 18px;
      border: none;
      border-radius: 10px;
      background-color: #ff69b4;
      color: white;
      cursor: pointer;
    }
    #mensajeFinal {
      position: absolute;
      top: 100%;
      left: 50%;
      transform: translateX(-50%);
      width: 80%;
      color: white;
      font-size: 24px;
      text-align: center;
      z-index: 2;
      transition: top 3s ease;
    }
    .flor {
      width: 50px;
      height: 50px;
      position: absolute;
      background: url('https://i.imgur.com/GeNqJZX.png') no-repeat center center;
      background-size: contain;
      animation: crecer 3s ease forwards;
    }
    @keyframes crecer {
      0% { transform: scale(0); }
      100% { transform: scale(1); }
    }
  </style>
</head>
<body>
  <audio id="musica" src="https://www.bensound.com/bensound-music/bensound-love.mp3" loop autoplay></audio>
  
  <canvas id="corazonCanvas"></canvas>

  <div id="inicio" class="centered">
    <h1>❤️ Te quiero mucho ❤️</h1>
    <p>El nombre de mi Princesa ❤❄</p>
    <div class="password-box">
      <input type="password" id="pass" placeholder="Escribe su nombre">
      <br>
      <button onclick="verificar()">Ingresar</button>
    </div>
  </div>

  <div id="segundaParte" class="hidden">
    <div id="mensajeFinal">
      <p>
        Camila, desde que te conocí, todo ha sido más bonito. Tu sonrisa ilumina mis días, tu risa me alegra el alma,
        y tu dulzura me hace querer ser mejor cada día. Adoro tu forma de ser, tu ternura, tu inteligencia y hasta tus
        enojos. Me encanta cómo hablas, cómo sueñas, cómo miras el mundo... Me haces sentir afortunado solo por tenerte cerca.
        Gracias por ser tú, por existir, por todo lo que me das con solo estar ahí. Te aprecio con el alma entera, y cada
        día me doy cuenta de que te quiero más. 💜
      </p>
    </div>
  </div>

  <script>
    // Partículas de corazón
    const canvas = document.getElementById('corazonCanvas');
    const ctx = canvas.getContext('2d');
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;

    let particles = [];
    function HeartParticle() {
      this.t = 0;
      this.radius = 4 + Math.random() * 2;
      this.color = Math.random() > 0.5 ? 'red' : 'cyan';
      this.x = 0;
      this.y = 0;
      this.update = function () {
        let t = this.t;
        this.x = 16 * Math.pow(Math.sin(t), 3);
        this.y = -(13 * Math.cos(t) - 5 * Math.cos(2 * t) -
                   2 * Math.cos(3 * t) - Math.cos(4 * t));
        this.x *= 10;
        this.y *= 10;
        this.t += 0.05;
      };
      this.draw = function () {
        ctx.beginPath();
        ctx.arc(canvas.width / 2 + this.x, canvas.height / 2 + this.y, this.radius, 0, Math.PI * 2);
        ctx.fillStyle = this.color;
        ctx.fill();
      };
    }

    for (let i = 0; i < 100; i++) {
      let p = new HeartParticle();
      p.t = Math.random() * Math.PI * 2;
      particles.push(p);
    }

    function animate() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      particles.forEach(p => {
        p.update();
        p.draw();
      });
      requestAnimationFrame(animate);
    }
    animate();

    // Verificar contraseña
    function verificar() {
      const pass = document.getElementById("pass").value.toLowerCase().trim();
      if (pass === "camila") {
        document.getElementById("inicio").classList.add("hidden");
        document.getElementById("segundaParte").classList.remove("hidden");
        document.getElementById("mensajeFinal").style.top = "10%";
        florecer();
        explosionCorazones();
        document.getElementById("musica").play();
      } else {
        alert("Nombre incorrecto 💔");
      }
    }

    // Flores moradas que crecen
    function florecer() {
      for (let i = 0; i < 15; i++) {
        const flor = document.createElement("div");
        flor.classList.add("flor");
        flor.style.left = Math.random() * window.innerWidth + "px";
        flor.style.top = Math.random() * window.innerHeight + "px";
        document.body.appendChild(flor);
      }
    }

    // Explosión de corazones
    function explosionCorazones() {
      for (let i = 0; i < 100; i++) {
        const div = document.createElement("div");
        div.style.position = "absolute";
        div.style.left = Math.random() * window.innerWidth + "px";
        div.style.top = Math.random() * window.innerHeight + "px";
        div.innerHTML = "💖";
        div.style.fontSize = (Math.random() * 20 + 20) + "px";
        div.style.animation = "subir 4s ease forwards";
        document.body.appendChild(div);
      }
    }

    // Animación de corazones
    const style = document.createElement("style");
    style.textContent = `
      @keyframes subir {
        0% { opacity: 1; transform: translateY(0); }
        100% { opacity: 0; transform: translateY(-200px); }
      }
    `;
    document.head.appendChild(style);
  </script>
</body>
</html>
